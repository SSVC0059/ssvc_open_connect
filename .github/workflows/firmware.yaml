name: Build Firmware

on:
  push:
    branches: [ '**' ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  packages: write
  contents: write

jobs:
  lint:
    runs-on: ubuntu-latest
    continue-on-error: true  # Линтер не будет блокировать весь workflow
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
          
      - name: Install Python requirements
        run: |
          pip install -r requirements.txt
          
      - name: Run linter
        run: |
          # Замените на вашу команду линтера
          platformio check --environment esp32dev || echo "Linter completed with warnings"
          
      - name: Upload lint results
        uses: actions/upload-artifact@v4
        if: always()  # Загружаем артефакты даже если линтер упал
        with:
          name: lint-results
          path: |
            **/.pio/**/*.log
            **/lint*.txt
          retention-days: 7

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - env_name: esp32-s3-devkitc-1-8m
          - env_name: esp32-s3-devkitc-1-8m-secure
          - env_name: esp32-s3-devkitc-1-16m
          - env_name: esp32-s3-devkitc-1-16m-secure

    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: 3.x

      - name: Extract version from tag or commit
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            version=${GITHUB_REF#refs/tags/v}
            version=${version//./-}
          else
            version="pr-${GITHUB_SHA:0:8}"
          fi
          echo "version=$version" >> $GITHUB_OUTPUT
          echo "Using version: $version"

      - name: Install Python requirements
        run: |
          pip install -r requirements.txt

      - name: Build firmware for ${{ matrix.env_name }}
        run: platformio run --environment ${{ matrix.env_name }}

      - name: Collect and archive artifacts for ${{ matrix.env_name }}
        if: github.event_name == 'push' && (startsWith(github.ref, 'refs/tags/v') || github.event.inputs.create_artifacts == 'true')
        run: |
          mkdir -p artifacts/firmware/${{ matrix.env_name }} artifacts/zips
          cp -r build/${{ matrix.env_name }}/* artifacts/firmware/${{ matrix.env_name }}/
          (cd artifacts/firmware/${{ matrix.env_name }} && zip -r ../../zips/${{ matrix.env_name }}.zip .)
          
          ls -la artifacts/firmware/${{ matrix.env_name }}/
          ls -la artifacts/zips/

      - name: Upload zip archive for ${{ matrix.env_name }}
        if: github.event_name == 'push' && (startsWith(github.ref, 'refs/tags/v') || github.event.inputs.create_artifacts == 'true')
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.env_name }}
          path: artifacts/zips/${{ matrix.env_name }}.zip
          if-no-files-found: error

      - name: Upload firmware binaries for ${{ matrix.env_name }}
        if: github.event_name == 'push' && (startsWith(github.ref, 'refs/tags/v') || github.event.inputs.create_artifacts == 'true')
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.env_name }}
          path: artifacts/firmware/${{ matrix.env_name }}/SSVC-Open-Connect_*
          if-no-files-found: error

      - name: Upload PR artifacts
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: pr-firmware-${{ matrix.env_name }}-${{ github.sha }}
          path: build/${{ matrix.env_name }}/SSVC-Open-Connect_*
          retention-days: 3

      - name: Upload ELF files
        uses: actions/upload-artifact@v4
        with:
          name: elf-files-${{ matrix.env_name }}
          path: build/elf/
          if-no-files-found: error
          retention-days: 90

  create-release:
    needs: [build, lint]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    
    # Загружаем артефакты для всех сборок
    steps:
      - name: Download zip artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Debug files
        run: |
          find artifacts -type f

      - name: Create Release
        id: create_release
        uses: elgohr/Github-Release-Action@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          title: Release ${{ github.ref_name || 'v0.0.0' }}
          tag: ${{ github.ref_name || 'v0.0.0' }}
          prerelease: true


      - name: Upload Release for ESP32S3-16m
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/firmware-esp32-s3-devkitc-1-16m/SSVC-Open-Connect_esp32-s3-devkitc-1-16m_${{ needs.build.outputs.version }}.bin
          asset_name: SSVC-Open-Connect_esp32-s3-devkitc-1-16m_${{ needs.build.outputs.version }}.bin
          asset_content_type: application/octet-stream


      - name: Upload MD5 Checksum for ESP32S3-16m
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/firmware-esp32-s3-devkitc-1-16m/SSVC-Open-Connect_esp32-s3-devkitc-1-16m_${{ needs.build.outputs.version }}.md5
          asset_name: SSVC-Open-Connect_esp32-s3-devkitc-1-16m_${{ needs.build.outputs.version }}.md5
          asset_content_type: text/plain

      - name: Upload Release for ESP32S3-16m (secure)
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/firmware-esp32-s3-devkitc-1-16m-secure/SSVC-Open-Connect_esp32-s3-devkitc-1-16m-secure_${{ needs.build.outputs.version }}.bin
          asset_name: SSVC-Open-Connect_esp32-s3-devkitc-1-16m-secure_${{ needs.build.outputs.version }}.bin
          asset_content_type: application/octet-stream


      - name: Upload MD5 Checksum for ESP32S3-16m (secure)
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/firmware-esp32-s3-devkitc-1-16m-secure/SSVC-Open-Connect_esp32-s3-devkitc-1-16m-secure_${{ needs.build.outputs.version }}.md5
          asset_name: SSVC-Open-Connect_esp32-s3-devkitc-1-16m-secure_${{ needs.build.outputs.version }}.md5
          asset_content_type: text/plain

      - name: Upload Release for ESP32S3-8m
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/firmware-esp32-s3-devkitc-1-8m/SSVC-Open-Connect_esp32-s3-devkitc-1-8m_${{ needs.build.outputs.version }}.bin
          asset_name: SSVC-Open-Connect_esp32-s3-devkitc-1-8m_${{ needs.build.outputs.version }}.bin
          asset_content_type: application/octet-stream

      - name: Upload MD5 Checksum for ESP32S3-8m
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/firmware-esp32-s3-devkitc-1-8m/SSVC-Open-Connect_esp32-s3-devkitc-1-8m_${{ needs.build.outputs.version }}.md5
          asset_name: SSVC-Open-Connect_esp32-s3-devkitc-1-8m_${{ needs.build.outputs.version }}.md5
          asset_content_type: text/plain

      - name: Upload Release for ESP32S3-8m (secure)
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/firmware-esp32-s3-devkitc-1-8m-secure/SSVC-Open-Connect_esp32-s3-devkitc-1-8m-secure_${{ needs.build.outputs.version }}.bin
          asset_name: SSVC-Open-Connect_esp32-s3-devkitc-1-8m-secure_${{ needs.build.outputs.version }}.bin
          asset_content_type: application/octet-stream

      - name: Upload MD5 Checksum for ESP32S3-8m (secure)
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/firmware-esp32-s3-devkitc-1-8m-secure/SSVC-Open-Connect_esp32-s3-devkitc-1-8m-secure_${{ needs.build.outputs.version }}.md5
          asset_name: SSVC-Open-Connect_esp32-s3-devkitc-1-8m-secure_${{ needs.build.outputs.version }}.md5
          asset_content_type: text/plain

      - name: Upload ESP32-S3-16M Zip Archive
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/esp32-s3-devkitc-1-16m/esp32-s3-devkitc-1-16m.zip
          asset_name: esp32-s3-devkitc-1-16m_${{ needs.build.outputs.version }}.zip
          asset_content_type: application/zip

      - name: Upload ESP32-S3-16M Zip Archive (secure)
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/esp32-s3-devkitc-1-16m-secure/esp32-s3-devkitc-1-16m-secure.zip
          asset_name: esp32-s3-devkitc-1-16m-secure_${{ needs.build.outputs.version }}.zip
          asset_content_type: application/zip

      - name: Upload ESP32-S3-8M Zip Archive
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/esp32-s3-devkitc-1-8m/esp32-s3-devkitc-1-8m.zip
          asset_name: esp32-s3-devkitc-1-8m_${{ needs.build.outputs.version }}.zip
          asset_content_type: application/zip

      - name: Upload ESP32-S3-8M Zip Archive (secure)
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/esp32-s3-devkitc-1-8m-secure/esp32-s3-devkitc-1-8m-secure.zip
          asset_name: esp32-s3-devkitc-1-8m-secure_${{ needs.build.outputs.version }}.zip
          asset_content_type: application/zip

  release-elf-files:
    needs: [ build, create-release ]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Download ELF artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: elf-files-*
          path: elf-artifacts
          merge-multiple: true

      - name: Archive all ELF files into a single zip
        run: zip -r elf-files.zip .
        working-directory: elf-artifacts

      - name: Upload ELF files to Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: elf-artifacts/elf-files.zip
          asset_name: elf-files_${{ needs.build.outputs.version }}.zip
          asset_content_type: application/zip